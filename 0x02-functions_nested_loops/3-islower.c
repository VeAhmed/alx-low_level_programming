#include "main.h"

/**
 * _islower - Entry point
 * @c: The character to ckeck
 * Description: checks if lower
 * Return: 1 if lower 0 if not
 */
int _islower(int c)
{
	if (c >= 97)
		return (1);
	return (0);
}
